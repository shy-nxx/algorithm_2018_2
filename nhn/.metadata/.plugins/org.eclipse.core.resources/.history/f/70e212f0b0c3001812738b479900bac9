package nhn_preTest;

import java.io.*;
import java.util.ArrayList;
import java.util.Scanner;
import java.util.Stack;

public class Q1 {
	public static void main(String[] args) throws Exception {
		Scanner scan = new Scanner(System.in);
		int n = Integer.parseInt(scan.nextLine()); //카드 개수
		int p = Integer.parseInt(scan.nextLine()); //섞는 횟수
		int[] ch = new int[p];
		
		for (int i = 0; i < p; i++) {
			ch[i]= Integer.parseInt(scan.nextLine()); //처음 카드섞이에서 제외할 갯수
		}

		Stack<Integer> card = new Stack<Integer>();
		
		for (int i = n; i >= 1; i--) {
			card.push(i);
		}

		int j = 1;
		ArrayList<Integer> pop_card = new ArrayList<>();
		
		for (int i = 0; i < ch.length; i++ ) {
			j = 1;
			while(j < (ch.length - ch[i])) {
				System.out.println(card.peek());
				pop_card.add(card.pop());
				j++;
			}
			for (int k = 0; k < j; k++) {
				card.push(pop_card.get(k));
			}
			for (int k = j; k < pop_card.size(); k++) {
				card.push(pop_card.get(k));
			}
			
		}
		for(int i = 0; i < 5; i++) {
			System.out.println(card.pop());
		}
		
	}
}
